# Quickstart: Import Session Log

## 概要

`llm_import` CLI に進捗ログ機能を追加。処理中の状況確認とデバッグが容易になる。

## 使用方法

### 基本実行

```bash
cd .dev
.venv/bin/python -m scripts.llm_import.cli --provider claude ../.staging/@llm_exports/claude/
```

### 出力例

```
═══════════════════════════════════════════════════════════
  LLM Import - 処理開始 [claude]
═══════════════════════════════════════════════════════════

処理対象: 422 会話
セッション: .staging/@plan/import_20260116_190000/

[████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░] 50/422 (11.8%)
[50/422] AIを活用した料理レシピ提案... Phase1 ✅ Phase2 ✅ (12.3s)

...

═══════════════════════════════════════════════════════════
  LLM Import - 処理結果 [claude]
═══════════════════════════════════════════════════════════

📊 処理結果サマリー
  ✅ 成功: 400
  ❌ エラー: 12
  ⏭️  スキップ: 10
  📋 Phase 2 未処理: 0

📁 Phase 別内訳
  Phase 1 完了: 422
  Phase 2 完了: 400

⏱️  処理時間
  総時間: 2時間15分30秒
  平均: 19.2秒/会話

📂 出力先
  ナレッジ: .staging/@index/
  セッションログ: .staging/@plan/import_20260116_190000/

詳細は状態ファイルを確認: .staging/@llm_exports/claude/.extraction_state.json
```

## セッションログの確認

### リアルタイム監視

```bash
# execution.log をリアルタイムで監視
tail -f .staging/@plan/import_*/execution.log
```

### 処理時間分析

```bash
# pipeline_stages.jsonl から平均処理時間を確認
cat .staging/@plan/import_*/pipeline_stages.jsonl | jq -s 'map(.timing_ms) | add / length'
```

### エラー確認

```bash
# errors.json を確認
cat .staging/@plan/import_*/errors.json | jq '.'
```

## ファイル構成

```
.staging/@plan/import_YYYYMMDD_HHMMSS/
├── session.json          # セッション情報
├── execution.log         # 人間可読ログ
├── pipeline_stages.jsonl # ステージ別処理記録
├── processed.json        # 成功リスト
├── errors.json           # エラーリスト
├── pending.json          # 未処理リスト
└── results.json          # 最終結果サマリー
```

## トラブルシューティング

### セッションログが作成されない

1. `.staging/@plan/` ディレクトリの権限を確認
2. ディスク容量を確認
3. 警告メッセージを確認（graceful degradation により処理は継続）

### プログレスバーが更新されない

- バックグラウンド実行時は `execution.log` を直接確認
- `tail -f` でリアルタイム監視推奨
